// - Entre colchetes = array

let itensArray = ["lápis", "caneta", "papel"];

// - Forma de selecionar objeto na Array:

console.log(itensArray[2]);

// - Entre chaves = objeto

let itensObjeto = {
   nome: "André",
   idade: 35,
   time: "Palmeiras"
}

// - Selecionar item no objeto

console.log(itensObjeto.nome);